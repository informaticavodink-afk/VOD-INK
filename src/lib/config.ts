import type { Tinta } from '../types';

/**
 * Catálogo opcional de tintas del estudio.
 *
 * Permanece vacío hasta que exista una fuente persistida y administrable. Los
 * datos de una intervención nunca deben autocompletarse con fixtures locales.
 */
export const TINTAS_PREDEFINIDAS: Tinta[] = [];
